<Window x:Class="TattooBookingManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TattooBookingManager.ViewModels;assembly=TattooBookingManager"
        mc:Ignorable="d"
        Title="Tattoo Booking Manager" Height="450" Width="800">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button Content="Добавить запись" Command="{Binding OpenEditWindowCommand}" Margin="10" />

        <DataGrid Grid.Row="1" ItemsSource="{Binding Appointments}" AutoGenerateColumns="False" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Клиент" Binding="{Binding Client.FirstName}" />
                <DataGridTextColumn Header="Мастер" Binding="{Binding Artist.Name}" />
                <DataGridTextColumn Header="Стиль" Binding="{Binding Style.StyleName}" />
                <DataGridTextColumn Header="Дата и время" Binding="{Binding AppointmentDateTime, StringFormat=dd.MM.yyyy HH:mm}" />
                <DataGridTextColumn Header="Продолжительность (мин)" Binding="{Binding Duration}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
